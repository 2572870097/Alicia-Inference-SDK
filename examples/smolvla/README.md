# SmolVLA Examples

SmolVLA-focused examples with instruction parameters exposed explicitly.

```bash
python examples/smolvla/01_load_and_inspect_smolvla.py \
  --checkpoint-dir /path/to/smolvla_checkpoint \
  --instruction "place the block into the tray"

python examples/smolvla/02_synthetic_control_loop_smolvla.py \
  --checkpoint-dir /path/to/smolvla_checkpoint \
  --instruction "place the block into the tray"

python examples/smolvla/03_manual_lifecycle_smolvla.py \
  --checkpoint-dir /path/to/smolvla_checkpoint \
  --instruction "place the block into the tray"
```
